<div class="container mt-5">
  <h1 class="mb-4">Edit Campaign for <%= @advertiser.name %></h1>
  
  <%= render 'campaign_form', campaign: @campaign, advertiser: @advertiser %>
  
  <%= link_to 'Back to Campaign List', admin_advertiser_campaigns_path(@advertiser), class: "btn btn-secondary mt-3" %>
</div>
